// my_alert.js

// Define a global function that calls alert()
function showAlert(message) {
    alert(message);
}

// You can also define more complex logic
function showCustomAlert(prefix, value) {
    alert(prefix + ": " + value);
}